services:
  # Backend API (Python/FastAPI)
  - type: web
    name: sudoku-api
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: postgresql://postgres:ujwalakshita@db.rqmfuglpqaelkxqifnxt.supabase.co:5432/postgres
        sync: false  # Set manually in Render dashboard with Supabase URL
      - key: PYTHON_VERSION
        value: "3.11"

  # Frontend (Static Site)
  - type: web
    name: sudoku-frontend
    env: static
    staticPublishPath: frontend
    buildCommand: echo "No build needed"
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
    routes:
      - type: rewrite
        source: /*
        destination: /Sudoku Difficulty Validator.html
